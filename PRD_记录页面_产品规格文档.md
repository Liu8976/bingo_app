# Bingo「记录」页面 —— 产品规格文档 (PRD)

> **版本**: v1.0  
> **作者**: 产品通  
> **日期**: 2026-07-06  
> **状态**: 草案（基于当前原型 v0 撰写，标注了已实现 vs 待实现）

---

## 目录

1. [问题陈述](#1-问题陈述)
2. [目标](#2-目标)
3. [非目标](#3-非目标)
4. [用户故事](#4-用户故事)
5. [功能需求](#5-功能需求)
6. [UI/交互规格](#6-ui交互规格)
7. [数据模型](#7-数据模型)
8. [成功指标](#8-成功指标)
9. [待解决问题](#9-待解决问题)
10. [时间线考量](#10-时间线考量)
11. [附录：当前实现状态](#11-附录当前实现状态)

---

## 1. 问题陈述

### 用户问题

Bingo 的三类目标用户（减脂新手、生活方式型、进阶训练者）在运动过程中面临一个共同的问题：**不知道自己有没有在变好**。

1. **「只看秤」的焦虑**：体重秤上的数字每天都在波动（水分、饮食、生理周期），单日数据造成的波动性焦虑会打击用户信心，甚至导致放弃。用户需要**趋势视角**来理解「整体在下降，今天是正常的波动」。

2. **饮食与运动的割裂**：用户很难把「吃了什么」和「练了什么」关联在一起看。大多数健身 App 的饮食记录和运动记录是两张独立的页面，用户需要在脑中自己做加减法。记录页应该提供**统一的数据仪表盘**。

3. **没有回顾、就没有成就感**：用户完成了一周的训练，但没有一个地方能直观地看到「这一周我做了什么」。没有回顾就没有正反馈循环，用户难以感知到坚持的价值。

4. **数据缺乏有趣的包装**：传统健身 App 的数据展示冷冰冰（数字+表格），与 Bingo 品牌的「游戏化、有趣、像健身搭子」调性完全不搭。数据展示也需要**品牌人格化**。

### 业务问题

记录页是 Bingo「留存飞轮」的关键一环：

- **复盘驱动复练**：用户看到「本周运动 3 次，比上周少了 1 次」→ 产生补练动力 → 跳转训练页。
- **数据建立信任**：用户看到体重趋势图和运动/饮食关联数据 → 相信 App 在帮自己变好 → 长期留存。
- **差异化竞争**：Keep、华为健康等竞品的记录页是「数字报表」，Bingo 有机会做成「你的身体故事」——有角色解说、有游戏化总结、有正向引导。

### 不解决的代价

- **留存断崖**：没有数据复盘的用户，7 日留存率预计显著低于有复盘的用户。
- **品牌定位落空**：「每一步都算数」（Slogan 备选）如果记录页不体现，品牌承诺就是空话。
- **功能孤岛**：首页收集的数据（运动分钟数、卡路里、饮食/体重记录标记）成为「一次性消耗品」，没有被二次利用产生价值。

---

## 2. 目标

| # | 目标 | 类型 | 衡量方式 |
|---|------|------|----------|
| G1 | 用户进入记录页后 5 秒内能获取「我这周整体在变好还是变差」的判断 | 用户体验 | 眼动测试 / 用户访谈中「第一眼看到了什么」的提及率 |
| G2 | 体重趋势图成为用户周均访问频率最高的记录模块（周访问≥3 次的用户占比 > 30%） | 留存 | 体重趋势模块的周均 PV 及 UV 占比 |
| G3 | 饮食+运动双卡并排 → 跳转至详情记录的比例 > 20% | 转化 | 饮食/运动卡片的「查看详情」点击率 |
| G4 | 记录页的「趋势提醒」文案成为留存情绪触点（用户反馈中正面提及率 > 15%） | 品牌 | 定性调研 / NPS 开放题词频分析 |
| G5 | 记录页数据完成首个完整的「采集→展示→洞察→行动」闭环（从页面内跳转至训练/饮食录入的比例 > 10%） | 业务闭环 | 记录页 → 训练页 / 饮食录入页的导航转化率 |

---

## 3. 非目标

| # | 非目标 | 原因 |
|---|--------|------|
| N1 | **详细饮食数据库（食物热量搜索引擎）** | 饮食数据录入属于独立功能模块（可能需要扫码/搜索/自定义），不在记录页的「展示层」范围内。记录页只展示已录入数据的聚合结果。 |
| N2 | **AI 营养师 / 饮食建议生成** | 需要 NLP 或规则引擎，属于高级增值功能，v1 不做。 |
| N3 | **训练动作详情回顾（逐帧回放）** | 属于训练播放页的历史回放功能，不在记录页范围。 |
| N4 | **社交分享战绩长图（自动生成海报）** | 属于「广场」Tab 的分享功能，记录页可以「导出数据」但不是「生成炫耀海报」。 |
| N5 | **设备数据自动同步**（Apple Health / 小米健康 / 华为健康） | 需要 Health Connect / HealthKit 集成，涉及隐私合规审批，属于独立项目。 |
| N6 | **数据导出（CSV/PDF）** | P2 规划，v1 不做。用户可在「我的」→「数据管理」中触发。 |

---

## 4. 用户故事

### 4.1 P0 用户故事（必须实现才能上线）

| 编号 | 用户故事 | 验收标准 |
|------|----------|----------|
| US-01 | 作为**减脂新手（用户A）**，我希望在记录页第一眼看到我的体重走势曲线，以便我知道「虽然今天重了 0.2kg，但这周整体在下降」，不再因为一天的波动而焦虑。 | 体重趋势图展示近 7 天/14 天/30 天的折线图；支持切换时间范围；数据来自真实体重记录；图表下方显示「当前体重」和「周期变化量」。 |
| US-02 | 作为**生活方式型用户（用户B）**，我希望在同一页面同时看到「今天吃了多少」和「今天练了多少」，以便我能直观地做「摄入 vs 消耗」的对比判断。 | 饮食记录卡展示今日总摄入（kcal）和距目标差值；运动记录卡展示今日运动时长（分钟）和消耗（kcal）；两卡在视觉上并排或上下紧邻，形成对比。 |
| US-03 | 作为**进阶训练者（用户C）**，我希望看到本周/本月/累计的运动数据汇总（总次数、总时长、总消耗），以便我能评估训练计划的执行效果。 | 运动记录卡支持切换「日/周/月」维度；展示累计运动次数、总时长、总消耗；数据来自真实运动记录。 |
| US-04 | 作为**所有用户**，我希望页面底部有一段角色化的「趋势提醒」，用肌肉伙伴的口吻对我这周的表现做一句话点评，以便我感觉被关注和被鼓励。 | 趋势提醒文案根据用户数据动态生成（如「这周比上周多动了 3 次，肌肉伙伴表示很欣慰。」），而非固定硬编码文本。 |

### 4.2 P1 用户故事（上线后快速跟进）

| 编号 | 用户故事 | 验收标准 |
|------|----------|----------|
| US-05 | 作为**减脂新手**，我希望体重趋势图上能点击某个数据点查看那天的具体数值，以便我回溯某天的异常变化原因。 | 点击图表数据点 → 浮层显示该日体重值和日期；支持左右滑动切换数据点。 |
| US-06 | 作为**生活方式型用户**，我希望饮食记录卡点击后能看到今日饮食明细（早餐/午餐/晚餐/加餐分别吃了什么），以便我发现热量超标的具体来源。 | 点击饮食记录卡 → 展开/跳转今日饮食明细列表，每条显示食物名称和热量。 |
| US-07 | 作为**进阶训练者**，我希望运动记录卡点击后能看到历史训练列表（按日期倒序），以便我回顾每次训练的详情。 | 点击运动记录卡 → 跳转训练历史列表；每条显示日期、训练类型、时长、消耗；点击某条进入该次训练的详情。 |
| US-08 | 作为**所有用户**，我希望页面右上角的 ⇧ 图标可以分享/导出当前记录页的摘要数据，以便我发到朋友圈或保存为图片。 | 点击 ⇧ 按钮 → 底部弹出分享选项（保存图片/分享到微信/复制文字摘要）；生成的分享图包含体重趋势图+核心数据+品牌水印。 |

### 4.3 P2 用户故事（远期规划，不在当前版本）

| 编号 | 用户故事 | 验收标准 |
|------|----------|----------|
| US-09 | 作为**减脂新手**，我希望体重趋势图支持设置目标体重线，以便我能直观地看到离目标还有多远。 | 体重图表中增加一条可配置的「目标体重」参考线（虚线/不同颜色）；在设置中可修改目标值。 |
| US-10 | 作为**所有用户**，我希望记录页支持添加身体围度记录（腰围/臀围/臂围等），并在趋势图中展示，以便我了解「体重没变但围度变小了」的真实变化。 | 围度记录作为体重趋势图的可选叠加维度；支持多维度切换查看。 |
| US-11 | 作为**进阶训练者**，我希望饮食与运动数据在同一图表中对比展示（摄入 vs 消耗柱状图），以便我一眼看出每日热量缺口。 | 「摄入 vs 消耗」对比柱状图，X 轴为日期，Y 轴为热量；两条柱子颜色不同（摄入橙色/消耗绿色）；底部显示净差值。 |
| US-12 | 作为**所有用户**，我希望记录页支持数据导出（CSV/PDF），以便我能把数据给私教或有其他用途。 | 在右上角菜单中增加「导出数据」选项；选择日期范围和导出格式；生成文件并触发系统分享。 |

---

## 5. 功能需求

### 5.1 页面结构总览

```
┌─────────────────────────────────┐
│  PageHeader                     │  ← 标题 + 副标题 + 分享/导出按钮
├─────────────────────────────────┤
│  WeightTrendCard                │  ← 体重趋势图 + 当前体重 + 周期变化
│  - MetricPill x2                │
│  - WeightChart (Canvas 折线图)   │
├─────────────────────────────────┤
│  FoodRecordCard                 │  ← 今日饮食摄入 + 剩余可吃 + 碗插图
│  - 摄入数值 + 距目标差值          │
│  - 装饰性 FoodBowlIllustration   │
├─────────────────────────────────┤
│  ExerciseRecordCard             │  ← 今日运动时长 + 消耗热量 + 图标
│  - 运动分钟数 + 消耗数值          │
│  - 维度切换（日/周/月）            │
├─────────────────────────────────┤
│  ReminderCard                   │  ← 角色化趋势提醒
│  - tip_fat_icon.png + 动态文案   │
└─────────────────────────────────┘
```

### 5.2 各模块需求详情

---

#### 5.2.1 PageHeader（页面头部）

| 属性 | 规格 |
|------|------|
| **标题** | 「身体档案」—— 用正式但不冰冷的词，暗示这是「属于你自己的数据档案」 |
| **副标题** | 「别只盯一天，看趋势才靠谱。」—— 引导用户建立趋势思维，降低单日数据焦虑 |
| **右侧按钮** | CircleIcon「⇧」，CardWhite 背景，72dp（当前占位）。点击 → 弹出分享/导出选项（P1）。 |
| **当前状态** | ✅ UI 已实现；❌ 按钮无点击回调、无功能 |
| **P0 待实现** | 无特殊 P0 依赖（⇧ 功能为 P1） |
| **P1 待实现** | ⇧ 按钮的分享/导出功能（见 US-08） |

---

#### 5.2.2 WeightTrendCard（体重趋势卡片）

| 属性 | 规格 |
|------|------|
| **标题** | SectionTitle「体重趋势」 |
| **指标区** | 两个 MetricPill 水平排列（weight=1f 等分）： |
|  | - Pill 1：「当前体重」→ 显示最新一次体重记录值（如「72.0 kg」，TextNavy 色） |
|  | - Pill 2：「近 X 天」→ 显示周期内变化量（如「-0.6 kg」，GrowthGreen 色上涨、红色下降） |
| **图表区** | Canvas 手绘折线图，150dp 高，圆角 20dp，Background 底色 |
|  | - 4 条水平网格线（BorderWarm，透明度 0.7，线宽 1.5f） |
|  | - 一条折线连接所有数据点（GrowthGreen 色，线宽 5f） |
|  | - 每个数据点：8px 白色外圆 + 5px GrowthGreen 内圆 |
|  | - 数据范围：7 个数据点（P0）、可选 14/30 天（P1） |
| **当前状态** | ✅ UI 完全实现；❌ 数据硬编码（7 个固定浮点值：「72.0 kg」「-0.6 kg」及图表值）；❌ 无 Y 轴标签；❌ 无 X 轴日期标签；❌ 无数据点交互 |
| **P0 待实现** | 1) 接入真实体重记录数据源 2) Y 轴显示数值刻度（kg）3) X 轴底部显示日期标签（月/日格式）4) 图表下方增加时间范围切换（7天/14天/30天） |
| **P1 待实现** | 1) 数据点点击交互（显示当日具体数值）2) 动画过渡（切换时间范围时折线平滑过渡）3) 目标体重参考线 |

##### 图表交互规范（P1）

```
- 点击数据点：浮层 Tooltip 显示「日期 + 体重值」
- 长按拖动：高亮最近的数据点并跟随手指移动
- 时间范围切换：三个 Tab（7天/14天/30天），切换时图表数据平滑过渡
- 空状态：无体重记录时，显示引导文案「还没记录过体重，去首页记录一次吧～」+ 跳转按钮
```

##### 图表 Y 轴刻度规则

```
- 自动计算 Y 轴范围：取数据范围 ± 5%（保证折线不触顶/触底）
- 刻度线数量：4 条（对应 0%/33%/66%/100% 位置）
- 显示格式：保留 1 位小数 + "kg"（如 "71.5 kg"）
- 位置：Y 轴标签在图表左侧，右对齐，12sp TextSecondary
```

---

#### 5.2.3 FoodRecordCard（饮食记录卡片）

| 属性 | 规格 |
|------|------|
| **标题** | SectionTitle「饮食记录」 |
| **布局** | 水平 Row：左侧文字区 + 右侧 FoodBowlIllustration（82dp） |
| **主数值** | 「今日总摄入 X,XXX kcal」—— TextNavy，粗体 |
| **副数值** | 「还可以吃 XXX kcal」或「已超出 XXX kcal」—— TextSecondary，13sp |
|  | - 未超标时：GrowthGreen 色文字「还可以吃 XXX kcal」 |
|  | - 已超标时：红色（#FF6B6B）文字「已超出 XXX kcal」 |
| **装饰插图** | Canvas 手绘碗图（椭圆底+弧形+彩色圆点代表食物），82dp |
| **当前状态** | ✅ UI 完全实现；❌ 数据硬编码（「1,420 kcal」「280 kcal」）；❌ 无点击交互；❌ 碗插图与数据无联动（始终相同） |
| **P0 待实现** | 1) 接入真实饮食记录数据 2) 根据用户设定的每日热量目标计算差值 3) 超标/未超标的颜色状态切换 |
| **P1 待实现** | 1) 点击卡片跳转/展开今日饮食明细 2) 碗插图随摄入比例变化（如装满程度/颜色） 3) 无饮食记录时的空状态引导 |

##### 热量差值计算规则

```
热量目标（每日）:
- 优先取用户自定义目标
- 未设置时：默认 1,700 kcal（女性）/ 2,000 kcal（男性）
- 差值 = 目标 - 当前摄入
  - 差值 > 0：展示「还可以吃 XXX kcal」（GrowthGreen）
  - 差值 = 0：展示「刚好达标，脂肪怪无话可说」（TextSecondary）
  - 差值 < 0：展示「已超出 XXX kcal」（#FF6B6B）
```

##### 空状态处理

```
当今日无饮食记录时：
- 主数值：「今天还没记饮食」
- 副数值：「记一下，让脂肪怪无处遁形。」
- 碗插图：空碗（仅白色椭圆轮廓，无彩色圆点）
- 点击卡片 → 跳转饮食录入页面
```

---

#### 5.2.4 ExerciseRecordCard（运动记录卡片）

| 属性 | 规格 |
|------|------|
| **标题** | SectionTitle「运动记录」 |
| **布局** | 水平 Row：左侧文字区 + 右侧 CircleIcon（↗，LightGreen 背景，GrowthGreen 文字，72dp） |
| **主数值** | 「今日运动 XX 分钟」—— TextNavy，粗体 |
| **副数值** | 「消耗 XXX kcal」—— TextSecondary，13sp |
| **维度切换** | 标题栏右侧增加「日/周/月」三个小标签（P0） |
|  | - 日视图：今日运动时长 + 消耗 |
|  | - 周视图：本周累计次数/总时长/总消耗（如「本周运动 4 次 · 共 150 分钟 · 消耗 1,200 kcal」） |
|  | - 月视图：本月累计次数/总时长/总消耗 |
| **当前状态** | ✅ UI 完全实现；❌ 数据硬编码（「40 分钟」「280 kcal」）；❌ 无点击交互；❌ 无维度切换 |
| **P0 待实现** | 1) 接入真实运动记录数据 2) 日/周/月维度切换 3) 空状态处理 |
| **P1 待实现** | 1) 点击卡片跳转训练历史列表 2) 右侧图标随运动完成度变化（未达标=灰色/达标=绿色/超标=金色） |

##### 维度切换数据规则

```
日视图：
- 运动时长 = sum(今日所有训练.时长)
- 消耗热量 = sum(今日所有训练.消耗)

周视图：
- 运动次数 = count(本周有训练记录的天数)
- 总时长 = sum(本周所有训练.时长)
- 总消耗 = sum(本周所有训练.消耗)

月视图：
- 运动次数 = count(本月有训练记录的天数)
- 总时长 = sum(本月所有训练.时长)
- 总消耗 = sum(本月所有训练.消耗)
```

##### 空状态处理

```
当日/周/月无运动记录时：
- 日视图：「今天还没动，脂肪怪在偷笑。」
- 周视图：「这周还没运动记录，周日截止来得及！」
- 月视图：「本月运动数据待填充，从今天开始吧。」
- 点击卡片 → 跳转训练页
```

---

#### 5.2.5 ReminderCard（趋势提醒卡片）

| 属性 | 规格 |
|------|------|
| **布局** | 水平 Row：左侧 tip_fat_icon.png（54dp）+ 右侧文字（标题 + 正文） |
| **标题** | 「趋势提醒：」—— TextNavy，粗体 |
| **正文** | 动态生成的品牌化文案—— TextNavy，13sp，行高 19sp |
| **图标** | `tip_fat_icon.png`，54dp |
| **卡片样式** | BingoCard，内边距 12dp，圆角 22dp |
| **当前状态** | ✅ UI 完全实现；❌ 文案硬编码（固定「别只盯一天，看趋势才靠谱。肌肉伙伴会帮你记住长期进步。」） |
| **P0 待实现** | 文案动态生成逻辑（基于实际数据计算趋势并匹配对应文案） |
| **P1 待实现** | 点击提醒卡片的行动引导（如「这周比上周少动了 2 次」→ 附带「去训练」按钮） |

##### 动态文案生成规则

```
文案模板（基于数据分析结果）:

体重趋势：
- 连续 7 天下降 → 「体重稳步下降，脂肪怪正在节节败退。继续保持这个节奏！」
- 整体下降但有波动 → 「趋势在变好，偶尔的波动是正常的。肌肉伙伴说：看线别看点。」
- 持平没有变化 → 「体重卡在平台期了？试试换个训练类型，脂肪怪可能已经适应了。」
- 上升 → 「最近体重有点抬头。是不是火锅局多了？不慌，肌肉伙伴陪你拉回来。」
- 无足够数据（< 3 条记录）→ 「再记录几次体重，肌肉伙伴就能帮你看出趋势了。坚持就是胜利！」

运动趋势：
- 本周 > 上周次数 → 「这周比上周多动了 2 次，肌肉伙伴表示很欣慰！」
- 本周 < 上周次数 → 「这周运动少了点。还来得及，周末把脂肪怪的嚣张气焰打下去。」
- 本周 = 上周次数 → 「运动节奏很稳定，脂肪怪最怕这种长期主义者。」

饮食趋势：
- 连续 3 天未超标 → 「连续 3 天管住嘴，脂肪怪的补给线快被切断了。」
- 今日超标 → 「今天吃得有点嗨，没关系，明天加一组训练就能拉回来。」

综合判断优先级：体重趋势 > 运动趋势 > 饮食趋势
当多个趋势都有显著变化时，取变化最大的维度生成文案。
```

---

#### 5.2.6 交叉模块：数据来源与同步

| 数据项 | 来源 | 更新频率 | P0/P1 |
|--------|------|----------|-------|
| 体重记录 | 首页「记录体重」入口 → 本地 DB（Room） | 实时（用户录入即更新） | P0 |
| 饮食摄入 | 饮食录入页面 → 本地 DB / 后端 API | 实时（用户录入即更新） | P0 |
| 运动数据 | 训练播放页完成 → 本地 DB / 后端 API | 实时（训练结束即更新） | P0 |
| 用户目标（热量/体重） | 「我的」→ 设置页 → 本地 DataStore | 设置变更时更新 | P0 |
| 趋势提醒文案 | 客户端本地计算（基于 DB 数据） | 每次进入页面时计算 | P0 |

---

### 5.3 合规与隐私

| 事项 | 说明 |
|------|------|
| **健康数据敏感度** | 体重、饮食、运动数据属于个人健康敏感数据，受《个人信息保护法》约束。本地存储需加密（EncryptedSharedPreferences / SQLCipher），上传云端需用户明确授权。 |
| **数据本地化** | v1 建议全量数据本地存储（Room DB），不上传服务器。降低隐私风险的同时也减少后端 API 开发压力。用户可在设置中手动触发云端备份（P2）。 |
| **分享安全** | 分享/导出功能（P1）生成图片时，需在隐私设置中提供「是否包含体重数据」的开关，默认关闭。 |
| **数据删除** | 「我的」页需提供「删除所有记录数据」选项，满足 GDPR/个保法的数据删除权要求。 |
| **未成年人保护** | 14 岁以下用户不展示排行榜数据，记录页数据仅自己可见（不做社交分发）。 |

---

## 6. UI/交互规格

### 6.1 视觉层级

```
优先级 1：体重趋势图（页面核心价值，最大视觉面积）
优先级 2：饮食记录 + 运动记录（并排对比，驱动日常行为）
优先级 3：趋势提醒（情感触点，推动正向反馈）
优先级 4：PageHeader（导航定位）
```

### 6.2 页面滚动行为

- 使用 `LazyColumn`（当前已实现为 `ScreenList` 封装）
- 所有内容在一个垂直滚动列表中
- 体重趋势图不吸顶，随页面自然滚动
- 滚动到底部时 ReminderCard 自然露出

### 6.3 按钮与手势交互

| 元素 | 触发行为 |
|------|----------|
| PageHeader ⇧ 按钮 | 弹出分享/导出选项（P1） |
| 体重趋势图数据点 | 点击 → Tooltip 显示日期+体重值；左右滑动 → 切换高亮数据点（P1） |
| 体重趋势图时间范围切换 | 点击「7天/14天/30天」标签 → 图表数据重新渲染 + 滑入动画 |
| 饮食记录卡片 | 点击 → 展开今日饮食明细（P1 展开模式）或跳转饮食列表页（P1 跳转模式） |
| 运动记录卡片 | 点击 → 跳转训练历史列表页（P1） |
| 运动记录日/周/月切换 | 点击标签 → 数值重新计算并刷新显示 |
| 趋势提醒卡片 | 点击 → 根据提醒内容跳转对应行动（训练页/饮食录入/体重录入）(P1) |
| 下拉刷新 | 下拉 → 刷新所有卡片数据（包括体重图、饮食、运动、提醒文案） |

### 6.4 刷新机制

- **自动刷新**：每次切换到「记录」Tab 时，如果上次数据加载超过 5 分钟，自动重新计算/拉取数据。
- **下拉刷新**（P1）：手动触发全量数据更新。
- **训练完成后刷新**：当从训练播放页返回（训练结束），记录页数据应自动更新。

### 6.5 加载与错误状态

| 状态 | 处理方式 |
|------|----------|
| 首次进入（无任何数据） | 所有卡片显示空状态引导（见各模块空状态设计），不显示骨架屏 |
| 有历史数据、加载增量数据 | 历史数据先展示，增量数据区域显示骨架屏（Shimmer） |
| 数据计算失败 | Toast「数据加载失败」；保留上次缓存数据展示；提供「重试」按钮 |
| 体重图表无数据（< 2 个点） | 显示引导插图 + 文案「再记录几次体重就能看到趋势了」；不渲染折线 |
| 饮食/运动卡片无今日数据 | 显示空状态（见各模块空状态设计） |

### 6.6 无障碍（Accessibility）

| 元素 | 无障碍要求 |
|------|-----------|
| 体重趋势图 | `contentDescription` = 「近 7 天体重趋势图，从 XX kg 到 XX kg，整体下降/上升/持平」|
| 数据点 | 每个数据点提供独立的 `contentDescription` |
| MetricPill | 朗读格式：「当前体重 72.0 公斤」—— 数值+单位 |
| 趋势提醒 | 朗读完整提醒文案，不折叠 |

---

## 7. 数据模型

### 7.1 体重记录

```kotlin
@Entity(tableName = "weight_records")
data class WeightRecord(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val weightKg: Float,            // 体重（kg），保留 1 位小数
    val recordedAt: Long,           // 记录时间（Unix 毫秒时间戳）
    val note: String? = null        // 备注（可选）
)
```

### 7.2 饮食记录（摘要，详情模型另见饮食模块 PRD）

```kotlin
@Entity(tableName = "food_records")
data class FoodRecord(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val date: String,               // 日期（yyyy-MM-dd）
    val totalCalories: Int,         // 当日总摄入（kcal）
    val targetCalories: Int,        // 当日热量目标（kcal）
    val mealType: String? = null    // 餐别（"breakfast"/"lunch"/"dinner"/"snack"）
)
```

### 7.3 运动记录（摘要，详情模型另见训练模块 PRD）

```kotlin
@Entity(tableName = "exercise_records")
data class ExerciseRecord(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val date: String,               // 日期（yyyy-MM-dd）
    val trainingTypeId: String,     // 训练类型 ID
    val durationMinutes: Int,       // 时长（分钟）
    val caloriesBurned: Int,        // 消耗热量（kcal）
    val completedAt: Long           // 完成时间（Unix 毫秒时间戳）
)
```

### 7.4 记录页状态（ViewModel）

```kotlin
data class RecordsPageState(
    // 体重
    val weightRecords: List<WeightRecord> = emptyList(),
    val currentWeight: Float = 0f,
    val weightChange: Float = 0f,           // 选定时间范围内的变化量
    val weightTrendDays: Int = 7,           // 趋势图显示天数

    // 饮食
    val todayCalorieIntake: Int = 0,
    val todayCalorieTarget: Int = 1700,
    val calorieRemaining: Int = 0,          // 正值=还可吃，负值=已超标

    // 运动
    val todayExerciseMinutes: Int = 0,
    val todayExerciseCalories: Int = 0,
    val weeklyExerciseCount: Int = 0,
    val weeklyExerciseMinutes: Int = 0,
    val weeklyExerciseCalories: Int = 0,
    val monthlyExerciseCount: Int = 0,
    val monthlyExerciseMinutes: Int = 0,
    val monthlyExerciseCalories: Int = 0,
    val exerciseViewMode: ExerciseViewMode = ExerciseViewMode.DAY,

    // 提醒
    val reminderText: String = "",

    // UI 状态
    val isLoading: Boolean = true,
    val errorMessage: String? = null
)

enum class ExerciseViewMode { DAY, WEEK, MONTH }
```

### 7.5 趋势提醒文案映射

```kotlin
data class TrendReminder(
    val category: TrendCategory,     // WEIGHT_DOWN, WEIGHT_UP, EXERCISE_MORE, etc.
    val title: String,               // "趋势提醒："
    val body: String,                // 动态生成的具体提醒文案
    val actionLabel: String?,        // 行动按钮文案（可选），如"去训练"
    val actionRoute: String?         // 行动按钮跳转路由（可选）
)

enum class TrendCategory {
    WEIGHT_TRENDING_DOWN,
    WEIGHT_TRENDING_UP,
    WEIGHT_PLATEAU,
    WEIGHT_FLUCTUATING,
    WEIGHT_INSUFFICIENT_DATA,
    EXERCISE_IMPROVING,
    EXERCISE_DECLINING,
    EXERCISE_STABLE,
    DIET_ON_TRACK,
    DIET_OVER_BUDGET,
    DIET_UNDER_TRACKING
}
```

---

## 8. 成功指标

### 8.1 北极星指标

**记录页 → 正向行为转化的比例**（从记录页跳转至训练/饮食录入/体重录入等核心行为的用户占比）

- 目标：≥ 15% 的记录页访问触发至少一次正向行为跳转

### 8.2 驱动指标

| 指标 | 定义 | 基线（待建） | 目标（30 天） |
|------|------|-------------|--------------|
| 体重趋势图查看完成率 | 图表的可视曝光（滚动至图表可见） / 页面 PV | 待测量 | > 85% |
| 维度切换率 | 运动记录「日/周/月」切换次数 / 页面 PV | 待测量 | > 20% |
| 饮食卡点击率 | 饮食记录卡片点击 / 页面 PV | 待测量 | > 18% |
| 趋势提醒阅读率 | ReminderCard 可视曝光 / 页面 PV | 待测量 | > 70% |
| 时间范围切换率 | 体重图「7天/14天/30天」切换次数 / 页面 PV | 待测量 | > 15% |

### 8.3 健康指标

| 指标 | 定义 | 警戒线 |
|------|------|--------|
| 页面加载时间 | 进入记录页到首屏渲染完成的 P90 时间 | < 1.5 秒 |
| 数据新鲜度 | 切换到记录 Tab 时数据更新时间差 | < 5 分钟（缓存阈值） |
| 空状态出现率 | 所有数据均为空的页面 PV / 总页面 PV | 新用户 Day 1 可接受 > 80%，Day 7 应 < 50% |
| 图表渲染性能 | WeightChart Canvas 绘制帧耗时 | < 16ms（保证 60fps） |

### 8.4 留存指标（滞后）

| 指标 | 观测窗口 |
|------|----------|
| 记录页周访问频次 | 7 天内平均访问天数（目标 > 4 天） |
| 体重记录持续率 | 首次记录后，第 7/14/30 天仍有记录的用户占比 |
| 记录页→7 日留存贡献 | A/B 测试：有完整记录页 vs 无记录页的用户 7 日留存差 |

---

## 9. 待解决问题

| # | 问题 | 负责人 | 阻塞级别 |
|---|------|--------|----------|
| Q1 | 体重记录的录入入口在哪里？首页是否有快速录入浮窗？记录页本身是否需要内嵌入录入功能？ | 产品 + 设计 | 阻塞 P0 |
| Q2 | 饮食记录的粒度如何设计？v1 是手动输入热量数值、拍照识别、还是从预设食物库选择？饮食数据模型需尽早确定。 | 产品 + 工程 | 阻塞 P0 |
| Q3 | 用户每日热量目标（1700/2000 kcal 默认值）的计算逻辑？是否基于身高/体重/年龄/性别的公式（如 Mifflin-St Jeor）自动推荐？ | 产品 + 算法 | 阻塞 P0 |
| Q4 | 体重趋势图的 Y 轴是固定刻度（如 60-80kg）还是动态自适应？自适应可能导致用户体重在图表上「看起来波动很大」的错觉。 | 产品 + 设计 | 非阻塞 |
| Q5 | 记录页数据是纯本地存储（Room）还是需要云端同步？如果只有本地，用户换手机数据会丢失。v1 建议纯本地 + 手动备份（P2）。 | 产品 + 工程 + 法务 | 阻塞 P0 |
| Q6 | 趋势提醒文案的生成规则是否需要运营后台可配置（方便 A/B 测试不同文案效果），还是纯客户端规则引擎？ | 产品 + 运营 | 非阻塞（P1） |
| Q7 | 记录页的权重是否应该比训练页更高（从数据驱动的留存角度看，复盘比发现更重要）？Tab 顺序和默认定位需要讨论。 | 产品 + 增长 | 非阻塞 |

---

## 10. 时间线考量

### 10.1 分阶段交付

```
Phase 1 — MVP（P0）— 2-3 周
├── 数据模型定义（WeightRecord / FoodRecord / ExerciseRecord）
├── Room DB 搭建（三张表 + DAO）
├── 体重记录录入 → 体重趋势图数据绑定
│   ├── Y 轴刻度标签
│   ├── X 轴日期标签
│   └── 7 天/14 天/30 天切换
├── 饮食记录录入 → 饮食卡片数据绑定
│   ├── 热量差值计算
│   └── 超标/未超标状态切换
├── 运动记录数据 → 运动卡片数据绑定
│   └── 日/周/月维度切换
├── 趋势提醒动态文案引擎
├── 各卡片空状态设计+实现
└── 页面加载状态（骨架屏）

Phase 2 — 增强体验（P1）— 2-4 周
├── 体重图数据点点击交互（Tooltip）
├── 饮食卡 → 今日饮食明细展开/跳转
├── 运动卡 → 训练历史列表跳转
├── PageHeader ⇧ 分享/导出功能
├── 下拉刷新
├── 趋势提醒行动按钮（跳转训练/饮食/体重录入）
├── 碗插图联动（随摄入比例变化）
└── 运动图标状态联动（未达标/达标/超标）

Phase 3 — 高级功能（P2）— 远期规划
├── 目标体重参考线
├── 围度记录及趋势图
├── 摄入 vs 消耗对比柱状图
├── 数据导出（CSV/PDF）
└── 云端备份与跨设备同步
```

### 10.2 依赖

| 依赖项 | 说明 | 影响 |
|--------|------|------|
| **体重录入入口** | 首页是否有快速录入？还是记录页内嵌录入？ | 阻塞 Phase 1（需确定数据来源） |
| **饮食录入模块** | 饮食数据的采集方式决定了 FoodRecord 的字段设计 | 阻塞 Phase 1 |
| **训练播放页** | 训练完成的回调需要将记录写入 Room DB | 阻塞 Phase 1（运动数据来源） |
| **Room DB 选型** | 需要增加 Room 依赖并设计数据库架构 | 阻塞 Phase 1 |
| **用户设置模块** | 热量目标、体重目标等用户偏好需要存储位置（DataStore） | 阻塞 Phase 1（热量差值计算） |

---

## 11. 附录：当前实现状态

> 以下记录当前代码原型（2026-07-06）的完成度，便于工程评估剩余工作量。

### 11.1 完成度矩阵

| 模块 | UI | 数据 | 交互 | 动态逻辑 | 整体 |
|------|-----|------|------|----------|------|
| PageHeader | ✅ 100% | N/A | ❌ 0% | N/A | 50% |
| WeightTrendCard | ✅ 100% | ❌ 硬编码 | ❌ 0% | ❌ 0% | 25% |
| FoodRecordCard | ✅ 100% | ❌ 硬编码 | ❌ 0% | ❌ 0% | 25% |
| ExerciseRecordCard | ✅ 100% | ❌ 硬编码 | ❌ 0% | ❌ 0% | 25% |
| ReminderCard | ✅ 100% | N/A | ❌ 0% | ❌ 0% | 25% |
| WeightChart（Canvas） | ✅ 100% | ❌ 硬编码 | ❌ 0% | ❌ 0% | 25% |
| FoodBowlIllustration | ✅ 100% | N/A | ❌ 0% | ❌ 0% | 25% |
| **整体** | **100%** | **0%** | **0%** | **0%** | **25%** |

### 11.2 硬编码数据速查

| 显示字段 | 硬编码值 | 所在文件及行号 |
|----------|----------|---------------|
| 页标题 | "身体档案" | RecordsScreen.kt:36 |
| 页副标题 | "别只盯一天，看趋势才靠谱。" | RecordsScreen.kt:37 |
| 当前体重 | "72.0 kg" | RecordsScreen.kt:58 |
| 7 天变化 | "-0.6 kg" | RecordsScreen.kt:59 |
| 体重图表数据 | `[0.82f, 0.68f, 0.58f, 0.52f, 0.43f, 0.34f, 0.45f]` | RecordsScreen.kt:103 |
| 今日摄入 | "1,420 kcal" | RecordsScreen.kt:71 |
| 剩余可吃 | "280 kcal" | RecordsScreen.kt:72 |
| 运动时长 | "40 分钟" | RecordsScreen.kt:85 |
| 消耗热量 | "280 kcal" | RecordsScreen.kt:86 |
| 趋势提醒 | "别只盯一天，看趋势才靠谱。肌肉伙伴会帮你记住长期进步。" | RecordsScreen.kt:47 |

### 11.3 代码文件清单

| 文件 | 角色 | 待修改内容 |
|------|------|-----------|
| `ui/screen/RecordsScreen.kt` | 记录页主 Composable（133 行） | 添加 ViewModel 状态绑定、点击回调、空状态、维度切换、文案动态生成 |
| `model/FitnessModels.kt` | 现有数据模型 | 新增 `WeightRecord`、`FoodRecord`、`ExerciseRecord`、`RecordsPageState` 等 |
| `mock/MockBingoData.kt` | Mock 数据 | 新增记录相关 Mock 数据（用于 Phase 1 开发阶段） |
| `ui/BingoVisuals.kt` | 共享组件 | 可能需要新增 `MetricPill` 的点击态、维度切换标签组件 |
| `ui/screen/HomeScreen.kt` | ReminderCard 定义位置 | 支持 ReminderCard 新增 `actionLabel`/`onAction` 参数 |
| `MainActivity.kt` | 导航 | 确保记录页相关子页面的路由通畅 |

### 11.4 缺少的依赖

| 依赖 | 用途 |
|------|------|
| Room (androidx.room) | 本地数据库——体重/饮食/运动记录的持久化存储 |
| DataStore (androidx.datastore) | 用户偏好存储——热量目标、体重目标等设置项 |
| ViewModel (androidx.lifecycle) | 页面状态管理——RecordsPageState 的数据加载与计算 |

---

> **产品经理不是需求搬运工，是价值判断官。**  
> 记录页的核心价值不是「展示数据」，而是「帮助用户看到自己的进步趋势并产生正向行动」。本文档定义的每一个功能和指标都服务于这个核心价值。
