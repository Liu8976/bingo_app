# Bingo「训练」页面 —— 产品规格文档 (PRD)

> **版本**: v1.0  
> **作者**: 产品通  
> **日期**: 2026-07-06  
> **状态**: 草案（基于当前原型 v0 撰写，标注了已实现 vs 待实现）

---

## 目录

1. [问题陈述](#1-问题陈述)
2. [目标](#2-目标)
3. [非目标](#3-非目标)
4. [用户故事](#4-用户故事)
5. [功能需求](#5-功能需求)
6. [UI/交互规格](#6-ui交互规格)
7. [数据模型](#7-数据模型)
8. [成功指标](#8-成功指标)
9. [待解决问题](#9-待解决问题)
10. [时间线考量](#10-时间线考量)
11. [附录：当前实现状态](#11-附录当前实现状态)

---

## 1. 问题陈述

### 用户问题

Bingo 是一款「用游戏化方式让减脂变得有趣」的 App，其品牌人格是**有趣但不轻浮的健身搭子**。目标用户（20-32 岁、以女性为主、容易三分钟热度）面临的核心痛点是：

1. **选择困难**：打开健身 App 后面对海量课程无从下手，常常在浏览中放弃。
2. **动力不足**：传统训练 App 的「课程列表」缺乏情感连接，用户难以建立「想练」的冲动。
3. **缺乏竞争与成就感**：独自训练缺少参照系，不知道自己处于什么水平，容易懈怠。
4. **训练类型单一**：大多数 App 只有「跑步」或「健身」两种模式，无法覆盖用户多样的运动场景（户外跑、室内燃脂、力量、拉伸、核心、快训）。

### 业务问题

训练页面是 Bingo 从「浏览」到「行动」的核心转化入口。没有训练页，用户无法：
- 发现并开始训练内容（核心转化漏斗断点）
- 被排行榜激励从而增加训练频次（留存机制缺失）
- 理解 Bingo 的品牌差异化（游戏化角色 IP 失去主要展示场景）

### 不解决的代价

- **用户流失**：首页吸引来的用户找不到训练入口，次日留存率下降。
- **品牌稀释**：角色 IP（脂肪怪/肌肉伙伴）没有承载场景，沦为装饰。
- **竞争对手领先**：Keep、每日瑜伽等产品已有成熟的训练发现体验，Bingo 需差异化追赶。

---

## 2. 目标

| # | 目标 | 类型 | 衡量方式 |
|---|------|------|----------|
| G1 | 用户进入训练页后 10 秒内能理解可选的训练类型并做出选择 | 用户体验 | 页面停留时间分布、热力图点击率 |
| G2 | Hero 推荐卡片成为训练页最主要的训练启动入口（CTR > 40%） | 业务 | Hero 卡片「开始训练」按钮点击率 |
| G3 | 排行榜驱动训练频次提升（有排行数据的用户周均训练次数 +30%） | 业务/留存 | A/B 测试：有排行 vs 无排行组训练频率对比 |
| G4 | 6 种训练类型每种的点击分布相对均匀（最低类型 ≥ 最高类型的 25%） | 内容策略 | 每种训练类型卡片的点击率分布 |
| G5 | 训练日历和奖杯成为用户回访的触点（入口点击率 > 8%） | 留存 | 日历/奖杯按钮的独立点击率 |

---

## 3. 非目标

| # | 非目标 | 原因 |
|---|--------|------|
| N1 | **训练内容播放页**（视频跟练/计时器） | 属于独立功能模块，不在训练入口页范围内。本页仅负责「发现与选择」。 |
| N2 | **训练计划生成器**（AI 定制周计划） | 属于高级功能，需用户数据积累和算法支持，v1 不做。 |
| N3 | **社交动态流**（好友训练状态/评论） | 属于「广场」Tab 的职责，训练页保持聚焦。 |
| N4 | **训练历史详情页**（单次训练数据回顾） | 属于「记录」Tab 的职责，日历按钮可导航至记录页的日历视图。 |
| N5 | **第三方设备数据同步**（Apple Watch/小米手环） | 需要 SDK 集成与隐私合规评审，属于独立项目，不在本页范围。 |
| N6 | **离线训练内容缓存** | 属于播放页的技术实现，不影响本页的浏览体验设计。 |

---

## 4. 用户故事

### 4.1 P0 用户故事（必须实现才能上线）

| 编号 | 用户故事 | 验收标准 |
|------|----------|----------|
| US-01 | 作为**减脂新手（用户A）**，我希望页面顶部有一个「今日推荐」训练，以便我不需要思考就能直接开始运动。 | Hero 卡片展示推荐训练名称/时长/强度/预估热量；「开始训练」按钮可点击并跳转至对应训练播放页。 |
| US-02 | 作为**生活方式型用户（用户B）**，我希望看到所有可选的训练类型（跑步/燃脂/力量/拉伸/核心/快速），以便根据我当下的状态和场景选择最合适的训练。 | 6 种训练类型以 2 列网格展示；每种显示图标+名称+一句趣味描述；点击进入对应训练列表或直接开始。 |
| US-03 | 作为**进阶训练者（用户C）**，我希望看到本周排行榜中的排名、距离和追赶进度，以便我有动力提升自己的排名。 | 排行榜卡片显示：当前排名、本周累计距离、距离前一名差距；数据来自真实 API 或本地计算（非硬编码）。 |
| US-04 | 作为**所有用户**，我希望页面头部有两个快捷入口（日历和奖杯），以便快速查看我的训练打卡记录和成就。 | 日历按钮→跳转至「记录」页的日历视图；奖杯按钮→跳转至「我的」页的成就/徽章区域。 |

### 4.2 P1 用户故事（上线后快速跟进）

| 编号 | 用户故事 | 验收标准 |
|------|----------|----------|
| US-05 | 作为**减脂新手**，我希望 Hero 推荐的训练每天自动更换，以便我不用每天重复同样的训练。 | Hero 卡片数据由推荐算法（或运营配置）每日更新，不是固定「全身燃脂训练」。 |
| US-06 | 作为**进阶训练者**，我希望排行榜有三个子榜单（进步榜/坚持榜/燃脂榜）可以切换查看，以便我能从不同维度了解自己的表现。 | 点击「进步榜」「坚持榜」「燃脂榜」标签 → 排行榜数据切换为对应维度的排名/数据。 |
| US-07 | 作为**所有用户**，我希望训练类型卡片有生动的视觉呈现（图标/动效），以便页面不显得死板，符合 Bingo 游戏化品牌调性。 | 训练类型图标具有品牌一致性（暖色调、卡通风格）；考虑 hover/点击态有微动效。 |
| US-08 | 作为**产品运营**，我希望 Hero 推荐训练可以后台配置（更换训练内容、更新文案），以便支持节日活动或季节性专题。 | 推荐训练支持 CMS 后台配置，不需要发版即可更新推荐内容和气泡文案。 |

### 4.3 P2 用户故事（远期规划，不在当前版本）

| 编号 | 用户故事 | 验收标准 |
|------|----------|----------|
| US-09 | 作为**减脂新手**，我希望训练类型按我的偏好排序，以便我常用的训练类型排在前面。 | 根据用户历史训练数据动态排序，使用频率高的训练类型置顶。 |
| US-10 | 作为**所有用户**，我希望下拉刷新时 Hero 卡片展示一个新的推荐训练，以便我有更多选择。 | 下拉刷新触发推荐算法重新计算，展示不同训练（不重复连续 3 次的推荐）。 |
| US-11 | 作为**进阶训练者**，我希望排行榜支持好友 PK 模式，以便我和朋友互相激励。 | 排行榜增加「好友」维度筛选，显示好友间的排名对比。 |
| US-12 | 作为**所有用户**，我希望训练类型卡片展示该类型我本周已完成次数，以便我了解自己的训练分布。 | 每个类型卡片角标显示「本周 X 次」。 |

---

## 5. 功能需求

### 5.1 页面结构总览

```
┌─────────────────────────────────┐
│  PageHeader                     │  ← 标题 + 副标题 + 日历/奖杯按钮
├─────────────────────────────────┤
│  TrainingHeroCard               │  ← 今日推荐（渐变卡片 + 插图 + 角色气泡）
├─────────────────────────────────┤
│  TrainingTypeGrid               │  ← 6 种训练类型（2 列网格）
├─────────────────────────────────┤
│  RunningLeaderboardCard         │  ← 本周跑路榜（排行数据 + 子榜单标签）
└─────────────────────────────────┘
```

### 5.2 各模块需求详情

---

#### 5.2.1 PageHeader（页面头部）

| 属性 | 规格 |
|------|------|
| **标题** | 「今天想怎么反击？」—— 品牌游戏化语言，用反问句降低说教感 |
| **副标题** | 「选一个训练，给脂肪怪来点压力。」—— 融入角色 IP，制造情感连接 |
| **右侧按钮 - 日历** | 圆形图标按钮（44dp），图标 `calendar_button.png`；点击跳转至「记录」Tab 的日历视图 |
| **右侧按钮 - 奖杯** | 圆形图标按钮（44dp），图标 `trophy_button.png`；点击跳转至「我的」Tab 的成就区域 |
| **当前状态** | ✅ UI 已实现；❌ 按钮无点击回调 |
| **P0 待实现** | 日历和奖杯的导航跳转逻辑 |
| **资源** | `nav_training_selected.png` / `nav_training_unselected.png` 用于底部 Tab 图标 |

---

#### 5.2.2 TrainingHeroCard（Hero 推荐卡片）

| 属性 | 规格 |
|------|------|
| **布局** | 高度 260dp 的圆角卡片（28dp），白色→浅橙色水平渐变背景 |
| **插图** | `illustration_training_hero.png`（肌肉伙伴角色），右对齐、偏移展示（x+18dp, y-15dp），宽度 210dp |
| **标签** | 「今日推荐」橙色 RewardChip（背景 LightOrange，文字 PrimaryOrange），宽度 140dp |
| **训练名称** | 粗黑体（FontWeight.Black）、20sp、TextNavy 色 |
| **训练信息** | 「30 分钟 · 中等强度 · 预计 220 kcal」—— 14sp、TextSecondary 色 |
| **角色气泡** | SpeechBubble 组件，白底暖色边框，文案「肌肉伙伴已热身，脂肪怪还在嘴硬。」，宽度 160dp |
| **CTA 按钮** | PrimaryGradientButton「开始训练」，全宽 45dp 高，橙→深橙渐变 |
| **当前状态** | ✅ UI 完全实现；❌ 数据硬编码（固定「全身燃脂训练」）；❌ 按钮无点击回调 |
| **P0 待实现** | 1) 推荐数据源（API 或本地推荐算法）2)「开始训练」按钮跳转至训练播放页 3) 角色气泡文案随推荐训练变化 |
| **P1 待实现** | 支持运营后台配置推荐内容；下拉刷新换一批推荐 |
| **设计参考** | `design_refs/4_training.png` |

##### 推荐算法设计（P1）

```
推荐策略：
- 默认推荐：由运营后台配置的「每日精选」训练
- 个性化推荐（需要用户数据）：
  - 排除用户最近 3 次完成的训练类型
  - 基于用户历史偏好（完成率最高的类型优先）
  - 周末/工作日差异化推荐（周末推荐户外跑，工作日推荐室内快速训练）
- 兜底策略：当无个性化数据时，从 6 种训练类型中轮换推荐
```

---

#### 5.2.3 TrainingTypeGrid（训练类型网格）

| 属性 | 规格 |
|------|------|
| **布局** | 2 列网格，列间距 10dp，每张卡片高度 60dp、圆角 20dp |
| **卡片设计** | 白底 + 暖色边框（BorderWarm，透明度 0.72）+ 左侧 35dp 图标 + 右侧标题/副标题 |
| **数量** | 6 种固定类型 |
| **P0 待实现** | 每种类型卡片的点击跳转（进入对应训练列表或直接开始训练） |
| **P1 待实现** | 动态排序（按用户使用频率）；角标展示「本周 X 次」 |

##### 训练类型定义

| # | 名称 | 副标题（趣味描述） | 图标资源 | 目标卡路里基准 |
|---|------|-------------------|----------|---------------|
| 1 | 户外训练 | 出门跑路 | `training_type_run.png` | 260 kcal/30min |
| 2 | 室内燃脂 | 客厅开战 | `training_type_burn.png` | 220 kcal/30min |
| 3 | 力量训练 | 肌肉在加班 | `training_type_strength.png` | 180 kcal/30min |
| 4 | 热身拉伸 | 身体要激活也要放松 | `training_type_stretch.png` | 80 kcal/30min |
| 5 | 核心训练 | 腹肌小宝贝在等你 | `training_type_core.png` | 150 kcal/30min |
| 6 | 快速训练 | 让脂肪排队下岗 | `training_type_quick.png` | 120 kcal/15min |

> **文案规范**：所有训练描述遵循品牌声音——「对话式、游戏化、不施加压力」（参考 `Bingo内容策略方案.md` 第三章）。

---

#### 5.2.4 RunningLeaderboardCard（排行榜卡片）

| 属性 | 规格 |
|------|------|
| **标题** | 「本周跑路榜」—— 用品牌语言替代「排行榜」 |
| **副标题** | 「注意：这里的跑路是物理意义上的跑路。」—— 品牌幽默 |
| **左侧奖杯区** | `training_leaderboard_trophy.png`，高度 148dp；叠加爆发装饰 `leaderboard_decor_burst.png` |
| **右侧数据区** | 3 项排行指标（每项包含 icon + 标签 + 数值）： |
|  | - 当前排名：「第 X 名」（GrowthGreen 色） |
|  | - 本周距离：「X.X km」（Blue 色） |
|  | - 追赶距离：「X.X km 可超过前一名」（PrimaryOrange 色） |
| **装饰元素** | 5 层装饰图（火花/爱心/彩纸/叶子/气泡），带半透明度和位置偏移 |
| **操作按钮** | OutlineOrangeButton「查看排行榜」 |
| **子榜单标签** | 3 个 RewardChip：「进步榜」（绿色）/「坚持榜」（黄色）/「燃脂榜」（紫色） |
| **当前状态** | ✅ UI 完全实现；❌ 数据硬编码（「第 18 名」「8.6 km」「1.2 km」均标注 TODO）；❌ 按钮和子榜单标签无点击回调 |
| **P0 待实现** | 1) 排行榜数据 API 集成 2)「查看排行榜」按钮跳转至完整排行榜页面 3) 无排行数据时的空状态处理 |
| **P1 待实现** | 1) 子榜单切换（进步榜/坚持榜/燃脂榜）2) 好友 PK 维度 3) 下拉刷新排行榜数据 |

##### 空状态处理（P0）

```
当用户本周无运动数据时：
- 排名显示「暂无排名」
- 距离显示「0 km」
- 追赶距离显示「动起来，超越昨天的自己！」
- 「查看排行榜」按钮仍可点击（展示其他用户的排行，激励作用）
```

##### 排行榜数据模型

```kotlin
data class WeeklyRankingData(
    val currentRank: Int,          // 当前排名（0 = 无排名）
    val totalParticipants: Int,    // 总参与人数
    val weeklyDistanceKm: Float,   // 本周累计距离（km）
    val distanceToNextKm: Float,   // 距离前一名差距（km，0 = 已是第一）
    val progressRank: Int,         // 进步榜排名
    val consistencyRank: Int,      // 坚持榜排名
    val calorieRank: Int           // 燃脂榜排名
)
```

---

#### 5.2.5 交叉模块：数据来源

| 模块 | 数据来源 | P0/P1 |
|------|----------|-------|
| Hero 推荐训练 | 运营后台配置（P0）+ 个性化推荐算法（P1） | P0 |
| 训练类型列表 | 静态配置（P0）+ 动态排序（P1） | P0 |
| 排行榜数据 | 后端 API（计算用户周运动数据并排名） | P0 |
| 角色气泡文案 | 与推荐训练联动，从配置表映射 | P0 |

---

### 5.3 合规与隐私

| 事项 | 说明 |
|------|------|
| **位置权限** | 户外训练若需要 GPS 轨迹记录，需用户授权位置权限（Android: `ACCESS_FINE_LOCATION`）。训练页本身不请求权限，仅在进入户外训练时触发。 |
| **健康数据** | 排行榜涉及用户运动数据聚合展示。需在隐私政策中说明：运动数据将用于排行榜排名计算，数据展示为匿名化或用户可控的昵称。 |
| **个人信息保护法** | 排行榜展示用户昵称和运动数据时，需提供「隐身模式」或「退出排行榜」选项。 |
| **数据存储** | 训练类型和推荐配置支持离线缓存，确保弱网环境下页面可正常展示（排行榜数据显示「加载中」或缓存数据）。 |

---

## 6. UI/交互规格

### 6.1 视觉层级

```
优先级 1：Hero 推荐卡片（最大面积、最高视觉权重、唯一渐变 CTA）
优先级 2：训练类型网格（核心浏览入口）
优先级 3：排行榜卡片（社交激励）
优先级 4：头部快捷按钮（辅助入口）
```

### 6.2 页面滚动行为

- 使用 `LazyColumn`（当前已实现为 `ScreenList` 封装）
- 所有内容在一个垂直滚动列表中
- Hero 卡片不吸顶，随页面自然滚动（保留完整插图展示面积）

### 6.3 按钮交互

| 按钮 | 触发行为 |
|------|----------|
| 日历图标 | 导航至「记录」Tab，自动定位到日历视图 |
| 奖杯图标 | 导航至「我的」Tab，自动定位到成就区块 |
| 「开始训练」 | 跳转至推荐训练对应的播放/详情页 |
| 训练类型卡片 | 跳转至该类型的训练列表页 |
| 「查看排行榜」 | 跳转至完整排行榜页面 |
| 子榜单标签 | 当前排行榜卡片内切换显示维度 |

### 6.4 刷新机制

- **下拉刷新**（P1）：触发重新请求 Hero 推荐 + 排行榜数据
- **自动刷新**：进入页面时判断上次数据缓存时间，超过 5 分钟自动拉取新数据

### 6.5 加载与错误状态

| 状态 | 处理方式 |
|------|----------|
| 首次加载中 | Hero 卡片/排行榜卡片显示骨架屏（Shimmer） |
| 数据加载失败 | Toast 提示「加载失败，请检查网络」；保留上次缓存数据展示 |
| 排行榜数据为空 | 显示空状态（见 5.2.4 节） |
| 推荐训练为空 | Hero 卡片展示兜底训练（运营配置的默认值） |

---

## 7. 数据模型

### 7.1 训练类型（静态配置）

```kotlin
data class TrainingType(
    val id: String,           // "outdoor_run", "indoor_burn", "strength", "stretch", "core", "quick"
    val name: String,         // "户外训练"
    val tagline: String,      // "出门跑路"
    val iconResId: Int,       // drawable 资源 ID
    val defaultDurationMin: Int,  // 默认时长（分钟）
    val defaultCalories: Int,     // 预估卡路里
    val intensity: String     // "低" / "中" / "高"
)
```

### 7.2 推荐训练（动态数据）

```kotlin
data class RecommendedTraining(
    val trainingId: String,
    val trainingName: String,
    val durationMin: Int,
    val intensity: String,
    val estimatedCalories: Int,
    val characterLine: String,   // 角色气泡文案
    val illustrationResId: Int   // Hero 插图资源
)
```

### 7.3 排行榜数据（动态数据）

```kotlin
data class WeeklyRankingData(
    val currentRank: Int,
    val totalParticipants: Int,
    val weeklyDistanceKm: Float,
    val distanceToNextKm: Float,
    val progressRank: Int,
    val consistencyRank: Int,
    val calorieRank: Int
)
```

### 7.4 页面状态（ViewModel）

```kotlin
data class TrainingPageState(
    val recommendedTraining: RecommendedTraining? = null,
    val trainingTypes: List<TrainingType> = emptyList(),
    val weeklyRanking: WeeklyRankingData? = null,
    val isLoading: Boolean = true,
    val errorMessage: String? = null,
    val selectedLeaderboardTab: LeaderboardTab = LeaderboardTab.RUNNING
)

enum class LeaderboardTab { RUNNING, PROGRESS, CONSISTENCY, CALORIE }
```

---

## 8. 成功指标

### 8.1 北极星指标

**训练页 → 训练开始的转化率**（页面访问 → 点击任一训练入口 → 开始训练）

- 目标：≥ 55% 的页面访问触发至少一次训练入口点击

### 8.2 驱动指标

| 指标 | 定义 | 基线（待建） | 目标（30 天） |
|------|------|-------------|--------------|
| Hero CTA 点击率 | Hero「开始训练」点击 / 页面 PV | 待测量 | > 40% |
| 训练类型点击分布 | 每种类型的点击占比标准差 | 待测量 | 标准差 < 15%（避免严重偏斜） |
| 排行榜查看率 | 「查看排行榜」点击 / 页面 PV | 待测量 | > 12% |
| 快捷入口使用率 | 日历 + 奖杯点击 / 页面 PV | 待测量 | > 8% |

### 8.3 健康指标

| 指标 | 定义 | 警戒线 |
|------|------|--------|
| 页面加载时间 | 首屏渲染完成时间（P90） | < 2 秒 |
| 页面崩溃率 | 训练页相关 crash / 页面 PV | < 0.1% |
| API 成功率 | 推荐/排行榜接口 200 响应率 | > 99.5% |
| 空状态出现率 | 排行榜无数据展示次数 / 排行榜展示次数 | 新用户前 7 天 < 30% |

### 8.4 留存指标（滞后）

| 指标 | 观测窗口 |
|------|----------|
| 训练页周访问频次 | 7 天内平均访问天数 |
| 训练页 → 训练完成转化 | 从训练页入口开始并完成训练的比例 |
| 排行榜对训练频次的影响 | A/B 测试（有排行 vs 无排行） |

---

## 9. 待解决问题

| # | 问题 | 负责人 | 阻塞级别 |
|---|------|--------|----------|
| Q1 | 排行榜是按「所有用户」还是「好友圈」排名？v1 建议全平台匿名排名 + 用户可选择退出的隐身模式。 | 产品 + 法务 | 阻塞 P0 |
| Q2 | Hero 推荐训练的内容从哪来？目前只有 6 种训练类型，是否每种类型下还有具体课程？需要确认训练内容的粒度结构。 | 产品 + 内容 | 阻塞 P0 |
| Q3 | 「开始训练」后的训练播放页是否已设计？训练页与播放页的导航参数约定？ | 产品 + 工程 | 阻塞 P0 |
| Q4 | 排行榜的「距离」指标在非跑步训练中如何计算？是否所有训练类型统一用「运动时长」而非「距离」？ | 产品 | 阻塞 P0 |
| Q5 | 6 种训练类型的图标是否需要进一步优化（当前是现有资源），是否需要设计统一风格的图标？ | 设计 | 非阻塞 |
| Q6 | 排行榜子榜单（进步榜/坚持榜/燃脂榜）的排名算法？进步榜=本周 vs 上周提升幅度？坚持榜=连续运动天数？燃脂榜=总消耗卡路里？ | 产品 | 非阻塞（P1） |

---

## 10. 时间线考量

### 10.1 分阶段交付

```
Phase 1 — MVP（P0）— 1-2 周
├── 训练类型点击跳转（到占位训练页或直接播放）
├── Hero「开始训练」按钮跳转
├── 推荐训练接入 Mock 数据（为后续 API 留接口）
├── 排行榜接入 Mock/本地数据
├── 日历/奖杯按钮导航
└── 加载状态与空状态处理

Phase 2 — 数据驱动（P1）— 2-4 周
├── Hero 推荐接入后端 API + 运营配置后台
├── 排行榜接入真实 API（含空状态）
├── 子榜单切换交互
├── 下拉刷新
└── 训练类型动态排序

Phase 3 — 增强体验（P2）— 远期规划
├── 个性化推荐算法
├── 好友 PK 排行
├── 训练类型角标（本周次数）
└── 动画/微交互增强
```

### 10.2 依赖

| 依赖项 | 说明 | 影响 |
|--------|------|------|
| 后端 API | 推荐训练接口、排行榜接口 | 阻塞 Phase 2 |
| 训练播放页 | 训练页所有 CTA 的目标页面 | 阻塞 Phase 1（需至少一个占位页） |
| 运营后台 | 推荐训练内容配置 | 阻塞 Phase 2 |
| 设计资源 | 空状态插图、骨架屏、图标优化 | 阻塞 Phase 1（可先用简单占位） |

---

## 11. 附录：当前实现状态

> 以下记录当前代码原型（2026-07-06）的完成度，便于工程评估剩余工作量。

### 11.1 完成度矩阵

| 模块 | UI | 数据 | 交互 | 整体 |
|------|-----|------|------|------|
| PageHeader | ✅ 100% | N/A | ❌ 0% | 66% |
| TrainingHeroCard | ✅ 100% | ❌ 硬编码 | ❌ 0% | 33% |
| TrainingTypeGrid | ✅ 100% | ⚠️ Mock | ❌ 0% | 50% |
| RunningLeaderboardCard | ✅ 100% | ❌ 硬编码+3 个 TODO | ❌ 0% | 33% |
| **整体** | **100%** | **15%** | **0%** | **40%** |

### 11.2 代码文件清单

| 文件 | 角色 | 待修改内容 |
|------|------|-----------|
| `ui/screen/TrainingScreen.kt` | 训练页主 Composable | 添加 ViewModel 状态绑定、点击回调、加载/空状态 |
| `mock/MockBingoData.kt` | Mock 数据 | 扩展 trainingTypes 为完整数据模型；新增推荐训练 mock |
| `model/FitnessModels.kt` | 数据模型 | 新增 TrainingPageState、WeeklyRankingData 等 |
| `ui/BingoVisuals.kt` | 共享组件 | 可能需要新增骨架屏组件 |
| `MainActivity.kt` | 导航 | 确保日历/奖杯按钮的导航路由通畅 |

### 11.3 资源文件清单

所有训练页相关资源文件详见 [Agent 探索报告]，包括：
- 1 张 Hero 插图
- 6 张训练类型图标
- 1 张排行榜奖杯
- 7 张排行榜装饰素材
- 2 张头部按钮图标
- 2 张导航栏图标

---

> **产品经理不是需求搬运工，是价值判断官。**  
> 本文档定义了训练页「做什么」和「为什么」，具体的「怎么做」请与工程团队协作确定技术方案。
